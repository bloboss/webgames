worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile on;
    tcp_nopush on;
    gzip on;
    gzip_types text/plain text/css application/javascript application/wasm;

    # 2048 - port 8001
    server {
        listen 8001;
        server_name _;
        root /usr/share/nginx/games/2048;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Generic Multi-Sudoku - port 8002
    server {
        listen 8002;
        server_name _;
        root /usr/share/nginx/games/generic-multi-sudoku;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Merge Game - port 8003
    server {
        listen 8003;
        server_name _;
        root /usr/share/nginx/games/merge-game;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Multi-Sudoku - port 8004
    server {
        listen 8004;
        server_name _;
        root /usr/share/nginx/games/multi-sudoku;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Number Match - port 8005
    server {
        listen 8005;
        server_name _;
        root /usr/share/nginx/games/number-match;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Sudoku - port 8006
    server {
        listen 8006;
        server_name _;
        root /usr/share/nginx/games/sudoku;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }

    # Water Sort - port 8007
    server {
        listen 8007;
        server_name _;
        root /usr/share/nginx/games/water-sort;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location ~* \.wasm$ {
            types { application/wasm wasm; }
        }
    }
}
